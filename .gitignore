__pycache__/
*.pyc
.firecrawl/
# Personal/admin-only tools, deliberately separate from the shared app -
# never distributed via the OTA updater or pushed to the team's repos.
standalone_tools/
chrome_profile*/
*.log
*.csv
*.zip
.env
python/
installer_output/
.auth_token
.grc_port
.update_log
autosave_results.json
gsc_profile/
extensions/**/secrets.txt
extensions/**/.DS_Store

# Sensitive data - API keys, secrets, credentials
config.json
.gsc_accounts
*.token
*.credentials
profiles/

# VPS tooling - lives in the separate seo-toolkit-vps-tools repo, never the
# public ota update-feed repo. Kept here locally as a convenience copy only.
vps_tools/
